server:
  port: 8888

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JAlayon/spring-cloud-config-server-repository
          default-label: main
          username: JAlayon
          password: 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
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: springCloud_Pwd!

logging:
  level:
    ROOT: INFO
    org.spring.framework.cloud.config: DEBUG
